<template>
  <div class="filter-bar">
    <slot />
  </div>
</template>

<script setup>
/**
 * Standard filter row wrapper — use inside pages below PageHeader.
 * Children should use .input-field with mt-0 (filter-bar resets margin).
 */
</script>
